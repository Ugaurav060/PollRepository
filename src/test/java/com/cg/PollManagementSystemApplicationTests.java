package com.cg;

/*import static org.junit.Assert.assertEquals;
import static org.mockito.Mockito.when;

import java.util.ArrayList;
import java.util.List;
import java.util.stream.Collectors;
import java.util.stream.Stream;

import org.junit.Test;
import org.junit.runner.RunWith;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.boot.test.context.SpringBootTest;
import org.springframework.boot.test.mock.mockito.MockBean;
import org.springframework.test.context.junit4.SpringRunner;

import com.cg.exception.PollManagementException;
import com.cg.model.Connections;
import com.cg.model.Request;
import com.cg.model.User_table;
import com.cg.pollrepository.ConnectionRepository;
import com.cg.pollrepository.PollRepository;
import com.cg.pollrepository.RequestRepository;
import com.cg.pollservice.PollService;*/

/*@RunWith(SpringRunner.class)
@SpringBootTest*/
public class PollManagementSystemApplicationTests {

	/*
	 * @Autowired private PollService service;
	 * 
	 * @MockBean private PollRepository userRepo;
	 * 
	 * @MockBean private RequestRepository requestRepo;
	 * 
	 * @MockBean private ConnectionRepository connectRepo;
	 * 
	 * User_table user=new User_table("aman12","aman12","Amanul");
	 * 
	 * 
	 * 
	 * 
	 * @Test public void getPassword() throws PollManagementException {
	 * when(userRepo.findByLogin("aman12")).thenReturn(user); assertEquals(user,
	 * service.getPassword("aman12","aman123")); }
	 * 
	 * @Test public void viewRequest() throws PollManagementException {
	 * when(requestRepo.findByRequestSentTo("aman12")).thenReturn(Stream. of(new
	 * Request(123, "aman12", "gaurav12"),new Request(124, "aman12",
	 * "george12")).collect(Collectors.toList())); assertEquals(2,
	 * service.viewRequest("aman12").size()); }
	 * 
	 * @Test public void viewfollower() throws PollManagementException {
	 * when(connectRepo.findByFollower("aman12")).thenReturn(Stream.of(new
	 * Connections(123, new User_table("amin", "terminator", "Arnold"),
	 * "gaurav12")).collect(Collectors.toList()));
	 * assertEquals(1,service.viewfollower("aman12").size()); }
	 * 
	 * @Test public void viewfollowing() throws PollManagementException {
	 * when(connectRepo.findByFollowing("aman12")).thenReturn(Stream.of(new
	 * Connections(123, new User_table("amin", "terminator", "Arnold"),
	 * "gaurav12")).collect(Collectors.toList()));
	 * assertEquals(1,service.viewfollowing("aman12").size()); }
	 * 
	 * 
	 */

}
